# Backend API base URL. Leave empty in dev to use the Vite proxy (/api -> http://localhost:8000).
VITE_API_URL=

# Optional: a default account ID to preload the dashboard with.
VITE_DEFAULT_ACCOUNT_ID=
