apiVersion: v1
kind: Service
metadata:
  name: {{ include "fintech-app.fullname" . }}
  labels:
    {{- include "fintech-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fintech-app.selectorLabels" . | nindent 4 }}
    component: api
